# Usage Examples

## Example 1 - Add Login Feature

Request:

Add a login feature including a login page, authentication API, JWT issuance, and basic login tests.

## Example 2 - Password Validation Bug

Request:

Passwords with leading or trailing spaces fail validation and should be fixed without changing the architecture.

## Example 3 - Production Token Failure

Request:

JWT validation failures are causing broad production errors and service should be restored with the smallest safe patch.
